spring:
  datasource:
    url: jdbc:tc:postgresql:14:///testdb
  jpa:
    hibernate:
      ddl-auto: create-drop
  ai:
    openai:
      api-key: test-key

knowhub:
  upload:
    directory: ./test-uploads
